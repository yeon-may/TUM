<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lightidea.tum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tum"
        android:label="TUM"
        android:roundIcon="@mipmap/ic_launcher_tum"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SixthYearActivity"/>
        <activity android:name=".FifthYearActivity" />
        <activity android:name=".FourthYearActivity" />
        <activity android:name=".ThirdYearActivity" />
        <activity android:name=".SecondYearActivity" />
        <activity android:name=".FirstYearActivity" />
        <activity android:name=".years_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>